

<div class="container text-center">
  <h1><%= @cocktail.name %></h1>



  <div class="jumbotron">
    <%= cl_image_tag @cocktail.photo, {:style => "height: 500px; width: 500px"} %>
   <h2>Ingredients</h2>

   <ul>
    <% @cocktail.doses.each do |dose| %>
    <li>
      <label><%= dose.ingredient.name %> ( <%= dose.description %> )</label>
      <%= link_to "🗑", dose_path(dose), method: :delete %>
    </li>
    <% end %>
  </ul>

  <p>Add an ingredient</p>
  <%= render 'doses/form', cocktail: @cocktail, dose: @dose %>
  <btn><%= link_to "Go back", cocktails_path %></btn>
</div>
</div>





