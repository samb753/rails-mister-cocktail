<%= simple_form_for([@cocktail, @dose]) do |f| %>
  <%= f.input :description, label: "Amount (cl)", id: "add-amount" %>
  <%= f.association :ingredient, id: "add-ingredient" %>
  <%= f.submit "Add", id: "cocktail-submit" %>
<% end  %>
