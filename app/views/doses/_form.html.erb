<%= simple_form_for([@cocktail, @dose]) do |f| %>
  <%= f.input :description, label: "Amount", id: "add-amount" %>
  <%= f.association :ingredient, id: "add-ingredient" %>
  <%= f.submit id: "cocktail-submit" %>
<% end  %>
